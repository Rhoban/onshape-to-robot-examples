% scale(1000) import("u_base_64.stl");

// Append pure shapes (cube, cylinder and sphere), e.g:

translate([0, 0, 3]) {
  cube([47, 27.5, 6], center=true);
}

// cylinder(r=10, h=10, center=true);
// sphere(10);
