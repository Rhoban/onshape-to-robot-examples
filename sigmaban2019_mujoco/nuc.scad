% scale(1000) import("nuc.stl");

// Append pure shapes (cube, cylinder and sphere), e.g:

translate([0,0,16])
cube([110, 110, 33], center=true);

// cylinder(r=10, h=10, center=true);
// sphere(10);
