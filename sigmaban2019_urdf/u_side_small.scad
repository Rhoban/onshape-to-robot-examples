% scale(1000) import("u_side_small.stl");

// Append pure shapes (cube, cylinder and sphere), e.g:

translate([15, 23, 1.5]) {
  cube([25, 44, 3], center=true);
}
 
// cylinder(r=10, h=10, center=true);
// sphere(10);
